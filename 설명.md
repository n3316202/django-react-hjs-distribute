### 1. .github/workflows/deploy.yml 생성

### 2. requirements.txt 생성 (로컬에서 생성)
pip list --format=freeze > requirements.txt

### 3. 배포시 에러 - 아래 설정
ALLOWED_HOSTS = ["*"]

```
추가)
INSTALLED_APPS = [
    ...
    'rest_framework',
    'api',
]
```
### 8. categories 모델을 생성 마이그레이션
   python manage.py makemigrations
   python manage.py migrate


### 전체 폴더 구조
django-react-hjs-distribute/
├── docker-compose.yml
├── docker/
│   └──nginx 
│   └──backend 
│   └──frontend 
├── backend/
│   ├── 
│   └── manage.py (및 Django 프로젝트)
├── frontend/
│   ├── vite.config.js
│   ├── index.html
│   └── src/
│       └── ...

2. 깃허브 올리기
.gitignore 추가