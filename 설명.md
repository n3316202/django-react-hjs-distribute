### 1. 아래 폴더에 supersuer.y 추가

backend\api\management\command

### 2. docker-compose.yml 아래 추가
python manage.py superuser && 

docker exec-it e9 /bin/bash

### 전체 폴더 구조
django-react-hjs-distribute/
├── docker-compose.yml
├── backend/
│   ├── 
│   └── manage.py (및 Django 프로젝트)
│   └── Dockerfile


2. 깃허브 올리기
.gitignore 추가